#pragma once

namespace ts::U {
    struct UFormat {
        UFormat() noexcept = default;
        
        ~UFormat() = default;

        // vars
        // initialize vars
        // code
        // export     
    };
}